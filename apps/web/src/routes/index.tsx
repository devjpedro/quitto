import { createFileRoute } from "@tanstack/react-router";

// Placeholder temporário (Task 1). As rotas reais entram nos Tasks 3 e 6.
export const Route = createFileRoute("/")({
  component: () => <div>Quitto SSR ok</div>,
});
