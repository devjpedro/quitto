# Fase 6c — LGPD: exportar dados + excluir conta — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao usuário portabilidade (exportar dados em JSON) e apagamento (excluir a conta — remove contratos próprios + purga seus arquivos no R2; preserva e anonimiza comprovantes em contratos de terceiros).

**Architecture:** Operações sempre sobre o usuário da sessão (sem params). A exclusão coleta as chaves R2 dos comprovantes de contratos próprios, deleta a linha `user` (cascata do banco), e purga o R2 (best-effort). O export agrega os dados via builder puro-ish. UI: página `/settings` com export + zona de perigo (frase digitada).

**Tech Stack:** Bun + Elysia + Drizzle (`bun test`); `@aws-sdk/client-s3` (`DeleteObjectsCommand`); React 19 + Vite (Vitest); Better Auth (`signOut`).

**Spec:** `docs/superpowers/specs/2026-06-13-fase-6c-lgpd-design.md`

**Git:** branch `feat/fase-6c-lgpd` a partir de `develop`; commit por tarefa; no fim, tudo verde → merge em `develop` e marcar a 6c no ROADMAP.

**Pré-requisitos:** Postgres (`DATABASE_URL`). Testes de R2 (`deleteObjects`, exclusão purgando objetos) exigem MinIO/S3 e usam `it.skipIf(!process.env.S3_ENDPOINT)`.

**Convenções:** código em inglês; UI pt-BR; sem literais (frase de confirmação em `@quitto/shared`); UI B2 via `frontend-design`; sem comentários óbvios.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `apps/api/src/db/schema.ts` (mod) | `proof.uploadedBy` → nullable + `set null` |
| `apps/api/src/lib/storage.ts` (mod) | `deleteObjects(keys)` |
| `apps/api/src/lib/account-export.ts` (criar) | `buildUserExport` (puro) + tipos |
| `apps/api/src/modules/account.ts` (criar) | `GET /api/me/export`, `DELETE /api/me` |
| `apps/api/src/app.ts` (mod) | registrar `accountModule` |
| `packages/shared/src/index.ts` (mod) | `DELETE_CONFIRM_PHRASE` |
| `apps/api/tests/account.test.ts` (criar) | export + delete (integração) + builder (unit) |
| `apps/api/tests/storage.test.ts` (mod) | `deleteObjects` |
| `apps/web/src/hooks/use-account.ts` (criar) | `useDeleteAccountMutation` |
| `apps/web/src/routes/settings.tsx` (criar) | página Conta |
| `apps/web/src/components/delete-account-dialog.tsx` (criar) | zona de perigo |
| `apps/web/src/router.tsx` (mod) | rota `/settings` |
| `apps/web/src/components/app-sidebar.tsx` (mod) | item "Conta" |
| `apps/web/tests/delete-account-dialog.test.tsx` (criar) | trava da frase |

---

## Task 1: Migração — `proof.uploadedBy` nullable + set null

**Files:**
- Modify: `apps/api/src/db/schema.ts`

- [ ] **Step 1: Alterar a coluna**

Em `apps/api/src/db/schema.ts`, no `proof`, troque:

```ts
    uploadedBy: text("uploaded_by")
      .notNull()
      .references(() => user.id, { onDelete: "cascade" }),
```

por:

```ts
    uploadedBy: text("uploaded_by").references(() => user.id, {
      onDelete: "set null",
    }),
```

- [ ] **Step 2: Ajustar quem lê `uploadedBy`**

`uploadedBy` agora é `string | null`. Rode o typecheck e ajuste os consumidores que assumiam `string` (provável: `modules/payments.ts` ao inserir o proof — a inserção continua passando `user.id` (string), o que é válido para coluna nullable; o problema é só na **leitura**). Se algum `select` expõe `uploadedBy` num response TypeBox como `t.String()`, troque para `t.Union([t.String(), t.Null()])`.

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto && bun run --filter @quitto/api typecheck`
Expected: PASS (após os ajustes).

- [ ] **Step 3: Gerar e aplicar a migration**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun run db:generate`
Expected: cria `apps/api/drizzle/00XX_*.sql` com `ALTER TABLE "proof" ... DROP NOT NULL` + recriação da FK como `ON DELETE SET NULL`. Confira o SQL gerado.

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun run db:migrate`
Expected: aplica sem erro.

- [ ] **Step 4: Suíte de payments não regrediu**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/payments.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/api/src/db/schema.ts apps/api/drizzle/ apps/api/src/modules/payments.ts
git commit -m "feat(api): proof.uploadedBy nullable + set null (preserva comprovante de terceiro)"
```

(Inclua no `git add` só os arquivos realmente tocados pelo ajuste do Step 2.)

---

## Task 2: `deleteObjects` no storage

**Files:**
- Modify: `apps/api/src/lib/storage.ts`
- Test: `apps/api/tests/storage.test.ts`

- [ ] **Step 1: Teste que falha**

Em `apps/api/tests/storage.test.ts`, adicione (siga o padrão de `skipIf` já usado no arquivo; reuse o client/helpers locais se houver, ou crie um upload direto como em `payments.test.ts`):

```ts
import { deleteObjects, headObject, presignUpload } from "../src/lib/storage";

const hasStorage = Boolean(process.env.S3_ENDPOINT);

describe("deleteObjects", () => {
  it("is a no-op for an empty list", async () => {
    await deleteObjects([]); // não lança
    expect(true).toBe(true);
  });

  it.skipIf(!hasStorage)("removes objects from the bucket", async () => {
    const key = `test/lgpd/${crypto.randomUUID()}.txt`;
    const url = await presignUpload(key, "text/plain");
    await fetch(url, { method: "PUT", body: "x", headers: { "content-type": "text/plain" } });
    await headObject(key); // existe
    await deleteObjects([key]);
    await expect(headObject(key)).rejects.toBeDefined(); // 404 após delete
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/storage.test.ts`
Expected: FAIL ("deleteObjects is not exported").

- [ ] **Step 3: Implementar**

Em `apps/api/src/lib/storage.ts`: adicione `DeleteObjectsCommand` ao import de `@aws-sdk/client-s3` e:

```ts
const DELETE_BATCH = 1000;

/** Deletes objects from the bucket in batches. No-op for an empty list. Best-effort: throws on AWS error. */
export async function deleteObjects(keys: string[]): Promise<void> {
  if (keys.length === 0) {
    return;
  }
  const { client: c, bucket } = s3();
  for (let i = 0; i < keys.length; i += DELETE_BATCH) {
    const batch = keys.slice(i, i + DELETE_BATCH);
    await c.send(
      new DeleteObjectsCommand({
        Bucket: bucket,
        Delete: { Objects: batch.map((Key) => ({ Key })) },
      })
    );
  }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/storage.test.ts`
Expected: PASS (delete real pula sem MinIO).

- [ ] **Step 5: Commit**

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/api/src/lib/storage.ts apps/api/tests/storage.test.ts
git commit -m "feat(api): storage.deleteObjects (purga em lote)"
```

---

## Task 3: Frase de confirmação no shared

**Files:**
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: Adicionar a constante**

Em `packages/shared/src/index.ts`, exporte:

```ts
/** Frase que o usuário digita para confirmar a exclusão da conta. */
export const DELETE_CONFIRM_PHRASE = "EXCLUIR";
```

- [ ] **Step 2: Typecheck + commit**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto && bun run --filter @quitto/shared typecheck`

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add packages/shared/src/index.ts
git commit -m "feat(shared): DELETE_CONFIRM_PHRASE"
```

---

## Task 4: Builder do export (puro)

**Files:**
- Create: `apps/api/src/lib/account-export.ts`
- Test: `apps/api/tests/account.test.ts` (parte 1)

- [ ] **Step 1: Teste que falha**

Crie `apps/api/tests/account.test.ts`:

```ts
import { describe, expect, it } from "bun:test";
import { buildUserExport, type ExportInput } from "../src/lib/account-export";

const input: ExportInput = {
  exportedAt: "2026-07-15T00:00:00.000Z",
  profile: { id: "u1", name: "Eu", email: "eu@e.com" },
  ownedContracts: [
    {
      contract: { id: "c1", title: "Aluguel" },
      installments: [{ sequence: 1, amountCents: 1000, dueDate: "2026-07-10", status: "pending" }],
      participants: [{ displayName: "Eu", role: "buyer" }],
      auditEvents: [{ type: "proof_submitted", createdAt: "2026-07-01T00:00:00.000Z" }],
      proofs: [{ fileName: "c.pdf", createdAt: "2026-07-01T00:00:00.000Z" }],
    },
  ],
  participatingContracts: [
    {
      contract: { id: "c2", title: "Venda" },
      mySlot: "seller",
      installments: [{ sequence: 1, amountCents: 2000, dueDate: "2026-08-10", status: "paid" }],
    },
  ],
  notifications: [
    { type: "payment_confirmed", contractId: "c1", installmentId: "i1", readAt: null, createdAt: "2026-07-02T00:00:00.000Z" },
  ],
};

describe("buildUserExport", () => {
  it("assembles the export object with all sections", () => {
    const out = buildUserExport(input);
    expect(out.profile.email).toBe("eu@e.com");
    expect(out.ownedContracts).toHaveLength(1);
    expect(out.ownedContracts[0]?.contract.title).toBe("Aluguel");
    expect(out.participatingContracts[0]?.mySlot).toBe("seller");
    expect(out.notifications).toHaveLength(1);
    expect(out.exportedAt).toBe("2026-07-15T00:00:00.000Z");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/account.test.ts`
Expected: FAIL ("Cannot find module ../src/lib/account-export").

- [ ] **Step 3: Implementar**

Crie `apps/api/src/lib/account-export.ts`. É um passthrough estruturado (mantém o handler fino e o formato testável):

```ts
export interface ExportProfile {
  id: string;
  name: string;
  email: string;
}
export interface ExportOwnedContract {
  contract: { id: string; title: string };
  installments: {
    sequence: number;
    amountCents: number;
    dueDate: string;
    status: string;
  }[];
  participants: { displayName: string; role: string }[];
  auditEvents: { type: string; createdAt: string }[];
  proofs: { fileName: string; createdAt: string }[];
}
export interface ExportParticipatingContract {
  contract: { id: string; title: string };
  mySlot: string;
  installments: {
    sequence: number;
    amountCents: number;
    dueDate: string;
    status: string;
  }[];
}
export interface ExportNotification {
  type: string;
  contractId: string;
  installmentId: string | null;
  readAt: string | null;
  createdAt: string;
}
export interface ExportInput {
  exportedAt: string;
  profile: ExportProfile;
  ownedContracts: ExportOwnedContract[];
  participatingContracts: ExportParticipatingContract[];
  notifications: ExportNotification[];
}
export type UserExport = ExportInput;

/** Assembles the user data export payload (pure: receives already-loaded data). */
export function buildUserExport(input: ExportInput): UserExport {
  return {
    exportedAt: input.exportedAt,
    profile: input.profile,
    ownedContracts: input.ownedContracts,
    participatingContracts: input.participatingContracts,
    notifications: input.notifications,
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/account.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/api/src/lib/account-export.ts apps/api/tests/account.test.ts
git commit -m "feat(api): builder do export de dados do usuário"
```

---

## Task 5: Endpoints `GET /api/me/export` e `DELETE /api/me`

**Files:**
- Create: `apps/api/src/modules/account.ts`
- Modify: `apps/api/src/app.ts`
- Test: `apps/api/tests/account.test.ts` (parte 2 — integração)

- [ ] **Step 1: Testes de integração que falham**

Adicione em `apps/api/tests/account.test.ts` (copie `signUpCookie`, `createContract`, e o helper `meId` — via `GET /api/me`):

```ts
import { and, eq } from "drizzle-orm";
import { app } from "../src/app";
import { db } from "../src/db/client";
import { contract, participant } from "../src/db/schema";

// ── cole signUpCookie + createContract (de tests/payments.test.ts) ──
async function meId(cookie: string): Promise<string> {
  const res = await app.handle(new Request("http://localhost/api/me", { headers: { cookie } }));
  return (await res.json()).id as string;
}

describe("GET /api/me/export", () => {
  it("requires auth", async () => {
    const res = await app.handle(new Request("http://localhost/api/me/export"));
    expect(res.status).toBe(401);
  });

  it("returns the caller's data as a JSON attachment", async () => {
    const cookie = await signUpCookie("exp");
    await createContract(cookie, false);
    const res = await app.handle(new Request("http://localhost/api/me/export", { headers: { cookie } }));
    expect(res.status).toBe(200);
    expect(res.headers.get("content-type")).toContain("application/json");
    expect(res.headers.get("content-disposition")).toContain("attachment");
    const body = await res.json();
    expect(body.ownedContracts.length).toBe(1);
    expect(body.profile).toBeDefined();
  });
});

describe("DELETE /api/me", () => {
  it("requires auth", async () => {
    const res = await app.handle(new Request("http://localhost/api/me", { method: "DELETE" }));
    expect(res.status).toBe(401);
  });

  it("deletes own contracts but preserves a third party's contract (slot unlinked)", async () => {
    // contrato de um terceiro, onde o 'mortal' participa como vendedor vinculado
    const ownerCookie = await signUpCookie("del-owner");
    const thirdContract = await createContract(ownerCookie, false);
    const mortalCookie = await signUpCookie("del-mortal");
    const mortalId = await meId(mortalCookie);
    await db.insert(participant).values({
      contractId: thirdContract,
      displayName: "Mortal",
      role: "seller",
      linkedUserId: mortalId,
    });
    // contrato próprio do 'mortal'
    const ownContract = await createContract(mortalCookie, false);

    const res = await app.handle(
      new Request("http://localhost/api/me", { method: "DELETE", headers: { cookie: mortalCookie } })
    );
    expect(res.status).toBe(200);

    // contrato próprio sumiu
    const own = await db.select().from(contract).where(eq(contract.id, ownContract));
    expect(own).toHaveLength(0);
    // contrato do terceiro sobrevive; o slot do mortal foi desvinculado
    const third = await db.select().from(contract).where(eq(contract.id, thirdContract));
    expect(third).toHaveLength(1);
    const slot = await db
      .select()
      .from(participant)
      .where(and(eq(participant.contractId, thirdContract), eq(participant.role, "seller")));
    expect(slot[0]?.linkedUserId).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/account.test.ts`
Expected: FAIL (rotas inexistentes).

- [ ] **Step 3: Implementar o módulo**

Crie `apps/api/src/modules/account.ts`:

```ts
import { eq, inArray } from "drizzle-orm";
import { Elysia, t } from "elysia";
import { db } from "../db/client";
import {
  auditEvent,
  contract,
  installment,
  notification,
  participant,
  proof,
  user as userTable,
} from "../db/schema";
import { buildUserExport } from "../lib/account-export";
import { requireAuth } from "../lib/session";
import { deleteObjects } from "../lib/storage";

const nowISO = () => new Date().toISOString();
const isoDateTime = (d: Date) => d.toISOString();

export const accountModule = new Elysia({ prefix: "/api" })
  .get("/me/export", async ({ request }) => {
    const { user } = await requireAuth(request.headers);

    const owned = await db.select().from(contract).where(eq(contract.ownerId, user.id));
    const ownedIds = owned.map((c) => c.id);

    const linked = await db
      .select({ contractId: participant.contractId, role: participant.role })
      .from(participant)
      .where(eq(participant.linkedUserId, user.id));
    const participatingIds = linked
      .map((l) => l.contractId)
      .filter((id) => !ownedIds.includes(id));

    const allIds = [...ownedIds, ...participatingIds];
    const items = allIds.length
      ? await db.select().from(installment).where(inArray(installment.contractId, allIds))
      : [];
    const people = ownedIds.length
      ? await db.select().from(participant).where(inArray(participant.contractId, ownedIds))
      : [];
    const events = ownedIds.length
      ? await db.select().from(auditEvent).where(inArray(auditEvent.contractId, ownedIds))
      : [];
    const ownedInstallmentIds = items
      .filter((it) => ownedIds.includes(it.contractId))
      .map((it) => it.id);
    const proofs = ownedInstallmentIds.length
      ? await db.select().from(proof).where(inArray(proof.installmentId, ownedInstallmentIds))
      : [];
    const notifs = await db.select().from(notification).where(eq(notification.userId, user.id));
    const participating = participatingIds.length
      ? await db
          .select({ id: contract.id, title: contract.title })
          .from(contract)
          .where(inArray(contract.id, participatingIds))
      : [];
    const slotByContract = new Map(linked.map((l) => [l.contractId, l.role]));

    const instOf = (contractId: string) =>
      items
        .filter((it) => it.contractId === contractId)
        .map((it) => ({
          sequence: it.sequence,
          amountCents: it.amountCents,
          dueDate: it.dueDate,
          status: it.status,
        }));

    const payload = buildUserExport({
      exportedAt: nowISO(),
      profile: { id: user.id, name: user.name, email: user.email },
      ownedContracts: owned.map((c) => ({
        contract: { id: c.id, title: c.title },
        installments: instOf(c.id),
        participants: people
          .filter((p) => p.contractId === c.id)
          .map((p) => ({ displayName: p.displayName, role: p.role })),
        auditEvents: events
          .filter((e) => e.contractId === c.id)
          .map((e) => ({ type: e.type, createdAt: isoDateTime(e.createdAt) })),
        proofs: items
          .filter((it) => it.contractId === c.id)
          .flatMap((it) =>
            proofs
              .filter((pr) => pr.installmentId === it.id)
              .map((pr) => ({ fileName: pr.fileName, createdAt: isoDateTime(pr.createdAt) }))
          ),
      })),
      participatingContracts: participating.map((c) => ({
        contract: { id: c.id, title: c.title },
        mySlot: slotByContract.get(c.id) ?? "viewer",
        installments: instOf(c.id),
      })),
      notifications: notifs.map((n) => ({
        type: n.type,
        contractId: n.contractId,
        installmentId: n.installmentId,
        readAt: n.readAt ? isoDateTime(n.readAt) : null,
        createdAt: isoDateTime(n.createdAt),
      })),
    });

    return new Response(JSON.stringify(payload, null, 2), {
      headers: {
        "content-type": "application/json; charset=utf-8",
        "content-disposition": 'attachment; filename="quitto-meus-dados.json"',
      },
    });
  })
  .delete("/me", async ({ request }) => {
    const { user } = await requireAuth(request.headers);

    // chaves R2 dos comprovantes de contratos PRÓPRIOS (serão apagados pela cascata)
    const keys = await db
      .select({ objectKey: proof.objectKey })
      .from(proof)
      .innerJoin(installment, eq(proof.installmentId, installment.id))
      .innerJoin(contract, eq(installment.contractId, contract.id))
      .where(eq(contract.ownerId, user.id));

    await db.delete(userTable).where(eq(userTable.id, user.id)); // cascata cuida do resto

    try {
      await deleteObjects(keys.map((k) => k.objectKey));
    } catch (err) {
      // best-effort: objeto órfão no R2 é vazamento menor, não erro de exclusão
      console.error("[delete-account] falha ao purgar R2", err);
    }

    return { ok: true as const };
  }, { response: t.Object({ ok: t.Literal(true) }) });
```

- [ ] **Step 4: Registrar no `app.ts`**

Importe `import { accountModule } from "./modules/account";` e adicione `.use(accountModule)`.

- [ ] **Step 5: Rodar e ver passar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test tests/account.test.ts`
Expected: PASS.

- [ ] **Step 6: Typecheck + commit**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto && bun run --filter @quitto/api typecheck`

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/api/src/modules/account.ts apps/api/src/app.ts apps/api/tests/account.test.ts
git commit -m "feat(api): exportar dados + excluir conta (purga R2 best-effort)"
```

---

## Task 6: Hook + diálogo de exclusão (web)

**Files:**
- Create: `apps/web/src/hooks/use-account.ts`
- Create: `apps/web/src/components/delete-account-dialog.tsx`
- Test: `apps/web/tests/delete-account-dialog.test.tsx`

- [ ] **Step 1: Teste que falha**

Crie `apps/web/tests/delete-account-dialog.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";

const mutate = vi.fn();
vi.mock("@/hooks/use-account", () => ({
  useDeleteAccountMutation: () => ({ mutate, isPending: false }),
}));

import { DeleteAccountDialog } from "../src/components/delete-account-dialog";

describe("DeleteAccountDialog", () => {
  it("keeps the confirm button disabled until the phrase is typed", async () => {
    render(<DeleteAccountDialog />);
    await userEvent.click(screen.getByRole("button", { name: /excluir conta/i }));

    const confirm = screen.getByRole("button", { name: /excluir definitivamente/i });
    expect(confirm).toBeDisabled();

    await userEvent.type(screen.getByLabelText(/digite/i), "EXCLUIR");
    expect(confirm).toBeEnabled();

    await userEvent.click(confirm);
    expect(mutate).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/web && bunx vitest run tests/delete-account-dialog.test.tsx`
Expected: FAIL (componentes não existem).

- [ ] **Step 3: Implementar o hook**

Crie `apps/web/src/hooks/use-account.ts`:

```ts
import { useMutation } from "@tanstack/react-query";
import { api } from "@/lib/api";
import { unwrap } from "@/lib/api-client";
import { signOut } from "@/lib/auth-client";

export function useDeleteAccountMutation() {
  return useMutation({
    mutationFn: () => unwrap(api.api.me.delete()),
    onSuccess: async () => {
      await signOut();
      window.location.href = "/login";
    },
  });
}
```

(Confirme o caminho do Eden para `DELETE /api/me`: `api.api.me.delete()`. Se o módulo `me`
existente colidir no tipo, o método `delete` ainda resolve pelo path `/api/me`.)

- [ ] **Step 4: Implementar o diálogo**

Crie `apps/web/src/components/delete-account-dialog.tsx`:

```tsx
import { DELETE_CONFIRM_PHRASE } from "@quitto/shared";
import { useState } from "react";
import { Dialog, DialogContent } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { useDeleteAccountMutation } from "@/hooks/use-account";

export function DeleteAccountDialog() {
  const [open, setOpen] = useState(false);
  const [phrase, setPhrase] = useState("");
  const del = useDeleteAccountMutation();
  const canDelete = phrase === DELETE_CONFIRM_PHRASE && !del.isPending;

  return (
    <>
      <button
        className="rounded-lg bg-destructive px-4 py-2 font-medium text-destructive-foreground text-sm transition-opacity hover:opacity-90"
        onClick={() => setOpen(true)}
        type="button"
      >
        Excluir conta
      </button>
      <Dialog onOpenChange={setOpen} open={open}>
        <DialogContent
          description="Esta ação é irreversível. Seus contratos e arquivos serão apagados."
          title="Excluir conta"
        >
          <label className="text-sm" htmlFor="confirm-phrase">
            Digite <strong>{DELETE_CONFIRM_PHRASE}</strong> para confirmar
          </label>
          <Input
            autoComplete="off"
            id="confirm-phrase"
            onChange={(e) => setPhrase(e.target.value)}
            value={phrase}
          />
          <button
            className="rounded-lg bg-destructive px-4 py-2 font-medium text-destructive-foreground text-sm disabled:opacity-50"
            disabled={!canDelete}
            onClick={() => del.mutate()}
            type="button"
          >
            Excluir definitivamente
          </button>
        </DialogContent>
      </Dialog>
    </>
  );
}
```

- [ ] **Step 5: Rodar e ver passar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/web && bunx vitest run tests/delete-account-dialog.test.tsx`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/web/src/hooks/use-account.ts apps/web/src/components/delete-account-dialog.tsx apps/web/tests/delete-account-dialog.test.tsx
git commit -m "feat(web): hook + diálogo de exclusão de conta (frase travada)"
```

---

## Task 7: Página `/settings` + nav

**Files:**
- Create: `apps/web/src/routes/settings.tsx`
- Modify: `apps/web/src/router.tsx`, `apps/web/src/components/app-sidebar.tsx`
- Test: `apps/web/tests/settings-page.test.tsx`

- [ ] **Step 1: Teste que falha**

Crie `apps/web/tests/settings-page.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

vi.mock("@/components/delete-account-dialog", () => ({
  DeleteAccountDialog: () => <div data-testid="delete-dialog" />,
}));

import { SettingsPage } from "../src/routes/settings";

describe("SettingsPage", () => {
  it("shows export link and the delete section", () => {
    render(<SettingsPage />);
    expect(screen.getByRole("link", { name: /exportar/i })).toHaveAttribute(
      "href",
      "/api/me/export"
    );
    expect(screen.getByTestId("delete-dialog")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/web && bunx vitest run tests/settings-page.test.tsx`
Expected: FAIL ("Cannot find module ../src/routes/settings").

- [ ] **Step 3: Implementar a página**

Crie `apps/web/src/routes/settings.tsx` (acabamento B2 via `frontend-design`):

```tsx
import { Download } from "lucide-react";
import { DeleteAccountDialog } from "@/components/delete-account-dialog";

export function SettingsPage() {
  return (
    <div className="mx-auto max-w-2xl p-6">
      <h1 className="mb-6 font-bold font-display text-2xl">Conta</h1>

      <section className="mb-6 rounded-xl border border-border bg-card p-5">
        <h2 className="font-display font-semibold">Exportar meus dados</h2>
        <p className="mt-1 mb-3 text-muted-foreground text-sm">
          Baixe um arquivo JSON com seus contratos, parcelas e notificações.
        </p>
        <a
          className="inline-flex items-center gap-1.5 rounded-lg border border-border px-3 py-2 text-sm hover:bg-muted"
          download
          href="/api/me/export"
        >
          <Download aria-hidden="true" className="size-4" />
          Exportar meus dados
        </a>
      </section>

      <section className="rounded-xl border border-destructive/30 bg-destructive/5 p-5">
        <h2 className="font-display font-semibold text-destructive">Zona de perigo</h2>
        <p className="mt-1 mb-3 text-muted-foreground text-sm">
          Excluir sua conta remove seus contratos e arquivos permanentemente.
        </p>
        <DeleteAccountDialog />
      </section>
    </div>
  );
}
```

- [ ] **Step 4: Rota + nav**

Em `apps/web/src/router.tsx`: importe `SettingsPage`, crie `settingsRoute` (`path: "/settings"`,
`getParentRoute: () => protectedRoute`, `component: SettingsPage`) e adicione em
`protectedRoute.addChildren([...])`.

Em `apps/web/src/components/app-sidebar.tsx`: importe o ícone `Settings` do `lucide-react` e
adicione ao `NAV`: `{ to: "/settings", label: "Conta", icon: Settings }`.

- [ ] **Step 5: Rodar e ver passar**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/web && bunx vitest run tests/settings-page.test.tsx`
Expected: PASS.

- [ ] **Step 6: Typecheck + commit**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto && bun run --filter @quitto/web typecheck`

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add apps/web/src/routes/settings.tsx apps/web/src/router.tsx apps/web/src/components/app-sidebar.tsx apps/web/tests/settings-page.test.tsx
git commit -m "feat(web): página Conta (exportar dados + zona de perigo)"
```

---

## Task 8: Verificação final + merge + roadmap

- [ ] **Step 1: Suíte da API**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/api && bun test`
Expected: verde (R2 real pula sem MinIO; suba MinIO p/ exercitar `deleteObjects`).

- [ ] **Step 2: Suíte do web**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto/apps/web && bunx vitest run`
Expected: verde.

- [ ] **Step 3: Typecheck + lint**

Run: `cd /home/buckz/Documentos/www/personal-projects/quitto && bun run typecheck && bun run lint`
Expected: PASS nos 3 pacotes.

- [ ] **Step 4: Smoke manual (dev, com MinIO)**

Suba `bun run dev`. Em `/settings`: "Exportar meus dados" baixa o JSON com seu conteúdo. Crie um
contrato com comprovante, depois exclua a conta digitando EXCLUIR → você é deslogado e cai no
login; confira no banco que o contrato sumiu e no MinIO que o objeto do comprovante foi purgado;
um contrato de terceiro onde você participava continua lá, com seu slot desvinculado.

- [ ] **Step 5: Marcar a 6c no ROADMAP + fechar a Fase 6**

Em `docs/superpowers/ROADMAP.md`, marque a **6c** concluída:
`` `plans/2026-06-13-fase-6c-lgpd.md` ✅ **concluído** (merge em `develop`; suite verde — export JSON, exclusão com cascata + purga R2 best-effort, comprovante de terceiro preservado/anonimizado, frase de confirmação) ``

- [ ] **Step 6: Commit do roadmap + merge**

```bash
cd /home/buckz/Documentos/www/personal-projects/quitto
git add docs/superpowers/ROADMAP.md
git commit -m "docs: marca Fase 6c concluída no roadmap"
git checkout develop
git merge --no-ff feat/fase-6c-lgpd -m "Merge: Fase 6c — LGPD (exportar dados + excluir conta)"
```

Expected: merge limpo; suites verdes em `develop`.

---

## Notas para o executor

- **Cascata é a fonte da verdade da exclusão:** o `db.delete(user)` derruba contratos próprios →
  parcelas/comprovantes/auditoria, notificações, sessões/contas, e seta `null` nos vínculos de
  terceiros. **Não** delete tabela por tabela na mão — confie na cascata (já no schema).
- **R2 best-effort, DB-first:** colete as chaves ANTES do delete; purgue DEPOIS; engula erro do
  R2 (logando). Nunca falhe a exclusão por causa do storage.
- **Sem params nos endpoints:** sempre o usuário da sessão — impossível atingir outra conta.
- **Sem literais:** a frase de confirmação vem de `DELETE_CONFIRM_PHRASE` (shared), usada no
  diálogo e implícita no backend (o backend não revalida a frase — a confirmação é UX no cliente;
  a proteção real é a sessão).
- **Eden:** `api.api.me.export.get()` para o export (ou âncora direta `/api/me/export` na UI — é o
  que a página usa); `api.api.me.delete()` para a exclusão.
- **Harness de teste da API:** reuse `signUpCookie`/`createContract` de `tests/payments.test.ts`.
- **Design B2:** `frontend-design` na página Conta; zona de perigo no tom destrutivo do tema.
